{
  "name": "apricot13",
  "version": "1.0.0",
  "description": "",
  "main": "index.html",
  "private": true,
  "scripts": {
    "start": "npx http-server ./public -i",
    "dev": "npx postcss --env=DEVELOPMENT src/styles/style.css -o public/style.css --watch",
    "build": "npx postcss --env=PRODUCTION src/styles/style.css -o public/style.css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "postcss-cli": "^11.0.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^10.4.12",
    "cssnano": "^5.1.13",
    "postcss": "^8.4.39",
    "postcss-import": "^15.0.0",
    "postcss-nested": "^6.0.0",
    "postcss-reporter": "^7.0.5"
  }
}
